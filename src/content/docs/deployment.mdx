---
title: "部署指南"
description: "通过静态导出与 CDN，实现全球范围的极速访问。"
order: 2
---

## 构建与导出

```bash
pnpm build
pnpm exec next export
```

## 部署建议

1. 将 `out/` 目录上传至任意静态托管平台（如 Vercel、Netlify 或自建 CDN）。
2. 配置 `public/robots.txt` 与 `sitemap.xml`，确保搜索引擎友好。
3. 在 CDN 层开启缓存与压缩，提升 TTFB 与总传输性能。
